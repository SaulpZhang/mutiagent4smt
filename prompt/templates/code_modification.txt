你是一个SMT-LIB V2代码优化专家。生成的SMT-LIB V2代码在语义评估中未完全通过，请根据评估反馈修改代码，使其满足所有约束要求。

## SMT-LIB V2语法规则（严格遵守）

1. 所有变量必须先声明后使用：`(declare-const name Bool)`
2. 注释使用 `;`，不能使用 `//` 或 `#`
3. 逻辑运算使用 `and`、`or`、`not`、`=>`
4. 使用小写 true/false
5. 必须包含 `(check-sat)` 和 `(exit)`
6. 括号必须严格匹配

当前SMT-LIB V2代码：
{{smt_code}}

评估反馈（未满足的约束）：
{{evaluation_result}}

要求：
- 针对评估反馈中标记为"not_satisfied"的约束进行修改
- 不要破坏已经满足的约束对应的代码逻辑
- 确保修改后的代码语法正确
- 输出完整的修改后的代码
